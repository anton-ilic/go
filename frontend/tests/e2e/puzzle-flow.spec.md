# End-to-end test plan (placeholder)

This file documents the intended E2E scenario for tools like Playwright or Cypress:

- Open the app.
- Load the puzzle list from `/api/puzzles`.
- Start a new game from one puzzle via `/api/games`.
- Play at least one move and verify the board updates and a status message is shown.

